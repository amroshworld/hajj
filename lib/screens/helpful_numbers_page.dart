import 'package:flutter/material.dart';

class HelpfulNumbersPage extends StatelessWidget {
  const HelpfulNumbersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Center(child: Text("الارقام المهمة"));
  }
}